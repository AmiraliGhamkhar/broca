*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# public/build IS committed on purpose: brocamed.ir deploys via cPanel Git
# Version Control and Node availability on the host is unconfirmed. Without
# committed artifacts a Node-less deploy renders the site with zero CSS
# (layouts/app.blade.php's @vite() throws on a missing manifest).
# Rebuild with `npm run build` and commit the result with the change.
/public/fonts-manifest.dev.json
/public/hot
/public/storage
# Runtime-provisioned placeholder media (see `php artisan broca:provision-media`)
/public/videos
/storage/*.key
/storage/pail

# Generated intermediates (raw AI-candidate hero originals); the optimized
# deliverables live in public/images/hero/.
/storage/meta
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
