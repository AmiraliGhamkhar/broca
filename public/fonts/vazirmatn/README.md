# Vazirmatn font files (placeholder)

Real font files must be placed here. The Phase 0 build tolerates their absence
because `app.css` uses `font-display: swap` and the `--font-sans` token lists
system-ui as a fallback.

**Required files:**
- `Vazirmatn-Regular.woff2` (weight 400)
- `Vazirmatn-Medium.woff2`  (weight 500)
- `Vazirmatn-Bold.woff2`    (weight 700)

**Source:** https://github.com/rastikerdar/vazirmatn/releases
License: OFL-1.1 (SIL Open Font License).

This entry is tracked in `SPEC.md` §15 (placeholder register).
